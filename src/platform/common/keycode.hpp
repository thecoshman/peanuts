#pragma once

namespace Peanuts{
    namespace Platform {
        enum class KeyCode{
            // TODO add in all 'generic' keycodes. eg:
            // A, B, C, B
            // Perhaps mouse and joystick buttons will be via another enum
        };
    }
}